/**
 * Legacy Xendit service placeholder.
 *
 * The payment gateway has been migrated to Tripay. This file remains only to
 * avoid breaking imports that might still reference the old service. It can
 * be deleted once every Xendit usage is removed.
 */

export {};
